# __PROJECT_NAME__ — setup notes

Generated by the `SETUP_NEW_PROJECT_SKILL` agent skill. Keep this file in
the project; it documents what the agent did and how to roll back.

## What's wired up

- **Frontend** — Vite, port `__PROJECT_PORT_FRONTEND__`. Proxies `/api` to the backend.
- **Backend** — Express + ts-node-dev, port `__PROJECT_PORT_BACKEND__`.
- **Database** — Postgres `__PROJECT_NAME__` on the i7's WSL2 instance.
- **Auth** — Firebase Google sign-in. Email allowlist in `server/.env` (`ALLOWED_EMAILS`).
- **Roles** — `admin`, `pro`, `guest`. `ADMIN_EMAILS` is the bootstrap-admin list.
- **Tunnel** — Cloudflare tunnel ingress rule for `__PROJECT_DOMAIN__`.
- **PM2** — entries in `F:/cloudflare/ecosystem.config.cjs`.

## Run locally

```powershell
# In two terminals:
cd server && npm run dev
cd client && npm run dev
```

## Public URL

https://__PROJECT_DOMAIN__

## Rollback (if you kill this project)

1. Remove PM2 entries from `F:/cloudflare/ecosystem.config.cjs` and `pm2 reload`.
2. Remove the ingress lines for `__PROJECT_DOMAIN__` from `~/.cloudflared/config.yml`.
3. Restart cloudflared service.
4. Remove the DNS record:
   `cloudflared tunnel route dns --overwrite-dns 3bab33c8-4282-4a1d-877d-e59c455e881d <other-hostname>`
   …or delete it in the Cloudflare dashboard (DNS tab → find the CNAME for `__PROJECT_DOMAIN__`).
5. Drop the DB:
   `wsl -d Ubuntu -- bash -c 'sudo -u postgres psql -c "DROP DATABASE __PROJECT_NAME__; DROP USER __PROJECT_NAME__;"'`
6. Remove the row from `F:/cloudflare/PORTS.md` (or mark it as freed).
7. Optionally archive/delete the GitHub repo.

## Hardening before public launch

- Replace `x-user-email` header trust with Firebase Admin SDK ID-token verification.
  See comments in `server/src/usersApi.ts`.
- Set CORS to a strict list (already done via `CLIENT_ORIGIN_URL`).
- Audit `ALLOWED_EMAILS` and `ADMIN_EMAILS` before opening to real users.
